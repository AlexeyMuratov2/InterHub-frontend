# Production: скопируйте в .env.production (файл в .gitignore).
# Vite подхватит .env.production при `npm run build`.
#
# Основной веб-интерфейс (деплой test_deploy):
# VITE_API_BASE_URL=https://interhub.online
#
# Telegram Mini App на поддомене — см. .env.miniapp.example и workflow deploy-frontend-miniapp.yml
VITE_API_BASE_URL=https://interhub.online
