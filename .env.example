# Копируйте в .env и заполните значения.
# Файл .env не коммитится в git.

# Базовый URL API. В dev при пустом значении запросы идут на тот же хост
# (используйте proxy в vite.config, например на http://localhost:8080).
VITE_API_BASE_URL=